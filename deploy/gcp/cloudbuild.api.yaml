substitutions:
  _IMAGE: gcr.io/$PROJECT_ID/reviewpulse-api

steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - docker/api/Dockerfile
      - -t
      - ${_IMAGE}
      - .

images:
  - ${_IMAGE}
